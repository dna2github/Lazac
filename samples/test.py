@anno(
   a, b, c
)
@anno2
def test():
   @anx
   def t2():
      """
      this is t2 document
      """
      x = lambda p: test(); x(1)
      # test for indent in next line
      x = lambda p: test()
      x = (lambda p: { 'a': p }, lambda p: { 'b': p }, lambda p: lambda q: {'factory': p })
      print 1
   if a:
      if a:
         b = 1
   else:
      c = 2
